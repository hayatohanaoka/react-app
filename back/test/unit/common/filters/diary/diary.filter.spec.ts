import { DiaryFilter } from '../../../../../src/common/filters/diary/diary.filter';

describe('DiaryFilter', () => {
  it('should be defined', () => {
    expect(new DiaryFilter()).toBeDefined();
  });
});
