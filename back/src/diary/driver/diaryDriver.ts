export class DiaryDriver {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  execute(title: string, content: string, date: string) {
    return {
      title: null,
      content: null,
      date: null,
    };
  }
}
